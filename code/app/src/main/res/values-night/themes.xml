<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.DevWrite" parent="Theme.Material3.Dark">
        <item name="iconTint">@color/white</item>
    </style>
    <style name="Theme.DevWrite.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>
    <style name="Theme.DevWrite.AppBarOverlay" parent="Theme.Material3.Dark.NoActionBar" />
</resources>